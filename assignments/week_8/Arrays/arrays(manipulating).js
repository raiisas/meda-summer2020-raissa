var counties = [
	'Belknap',
	'Strafford',
	'Carroll',
	'Rockingham'
]; 

counties[2] = "Alameda" // manipulate or changing a value

counties[counties.length] = Solano //counties.length is an expression

counties.push("Santa Clara") // add

counties.pop("Carroll") // delete

counties.splice(2, 1) // get rid of something in the middle
// 2 is the index 
// 1 is the item you want to remove
